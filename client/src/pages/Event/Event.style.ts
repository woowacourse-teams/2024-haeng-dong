import {css} from '@emotion/react';

export const EventStyle = () =>
  css({
    display: 'flex',
    flexDirection: 'column',
    gap: '1rem',
  });

export const ReceiptStyle = () =>
  css({
    display: 'flex',
    flexDirection: 'column',
    gap: '8px',
    padding: '0 8px',
  });
