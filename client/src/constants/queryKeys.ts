const QUERY_KEYS = {
  steps: 'steps',
  event: 'event',
  allMembers: 'allMembers',
  currentMembers: 'currentMembers',
  reports: 'reports',
  billDetails: 'billDetails',
};

export default QUERY_KEYS;
