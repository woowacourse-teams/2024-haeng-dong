{
  "name": "client",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npx lerna run test",
    "build": "npx lerna run build",
    "version": "npx lerna version --no-private",
    "graph": "npx nx graph"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}