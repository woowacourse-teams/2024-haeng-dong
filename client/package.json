{
  "name": "client",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npx lerna run build --scope=haengdong-design && npx lerna run dev --scope=haengdong-client",
    "lint": "npx lerna run lint",
    "test": "npx lerna run test --scope=haengdong-client",
    "build": "npx lerna run build",
    "publish": "npx lerna publish from-package --scope=haengdong-design",
    "version": "npx lerna version --no-private",
    "graph": "npx nx graph"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}