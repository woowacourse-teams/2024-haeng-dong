spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:database;MODE=MYSQL
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        default_batch_fetch_size: 30
        format_sql: true
    show-sql: true
